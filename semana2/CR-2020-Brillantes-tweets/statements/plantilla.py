# Leemos el número de Tweets
n = 
# Evaluamos cada tweet
for  in :
    # Guardaremos en una variable el contador de caracteres
	con = 
    # Leemos el tweet
	t = input()
    # Contamos caracteres
	for  in :
		if :
			con = 
		else:
			con =   
    # Verificamos si el tweet es correcto o no
	if :
		print("Correcto")
	else:
		print("Incorrecto")
    # Imprimimos el número de caracteres del tweet
	print()
